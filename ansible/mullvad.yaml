- name: "Install latest mullvad for Debian"
  hosts: localhost
  connection: local
  become: true
  tasks:
    - name: "Install Packages"
      register: aptout
      apt:
        state: latest
        deb: https://mullvad.net/download/app/deb/latest/

    - debug: var=aptout
